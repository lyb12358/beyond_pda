import 'package:flutter/material.dart';

class OnlineGridviewPage extends StatelessWidget {
  const OnlineGridviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('门店盘点')),
    );
  }
}
